"""
Shared fixtures will land here.

Critical coverage still needed:
- authentication (missing / invalid / valid JWT)
- workspace scoping (user A cannot read user B)
- soft delete and restore
- folder depth and non-empty folder delete
"""
